const title = 'monitor 20 pulgadas';
const price = ' 20 dollars';

console.log(title.concat(price));
console.log(title + price + "this is product");
console.log(`El product ${product} el precio es $ ${price}`);